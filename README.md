# Mini-File-Search-Utility-in-Linux
Mini File Search Utility A lightweight Bash command-line tool to search files by name, extension, or size in any directory. Built using the filesystem library with support for recursive search and clean console output.
